{{> header}}

<div class="container home some">
    <img class="det-img" src="{{imageURL}}" />
    <div class="desc">
        <h2 class="display-5">{{title}}</h2>
        <p class="infoType">Description:</p>
        <p class="idea-description">{{ description }}</p>
        <p class="infoType">Likes: <large>{{ likes }}</large>
        </p>
        <p class="infoType">Comments:</p>
        {{#if ideas}}
        <ul>

            {{#each comments}}
            <li class="comment">{{username}} : {{this}}</li>
            {{/each}}
        </ul>
        {{else}}
        <h5>No comments yet :(</h1>
            {{/if}}
    </div>

    {{#if canDonate}}
    <form class="text-center" method="" action="">
        <textarea class="textarea-det" name="newComment" id=""></textarea>
        <button type="submit" class="btn detb">Comment</button>
        <a class="btn detb" href="#/cause/donate/{{id}}">Like</a>
    </form>
    {{else}}
    <div class="text-center">
        <a class="btn detb" href="#/cause/close/{{id}}">Delete</a>
    </div>

    {{/if}}
</div>


{{> footer}}


<!-- Details View (Become a donor) -->
{{!-- <div class="container" style="margin-bottom: 2%; margin-top:5%;">
        <div class="row">
            <div class="modal-content col-lg-12">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
                    <h4 class="modal-title" id="myModalLabel">Make a donation</h4>
                </div>
                <div class="modal-body">
                    <center>
                        <img style="border-radius: 5px;" src="{{pictureUrl}}" name="aboutme" width="500" height="auto" border="0" class="img-circle">
                        <h3 class="media-heading">{{ cause }}</h3>
                        <span>Collected</span>
                        <strong class="label text-success">${{collectedFunds}}</strong>
                        <span> from </span>
                        <strong class="label text-info">${{neededFunds}}</strong>
                    </center>
                    <hr>
                    <center>
                        <p style="word-wrap: break-word;" class="text-left"><strong>Description: </strong><br> {{description}}
                        </p>
                        <br>
                        <p style="word-wrap: break-word;" class="text-left"><strong>Donors:</strong><br>
                            {{#each donors}}
                            <span>{{this}}</span>
                            {{/each}}
                        </p>
                    </center>
                </div>
                <div class="modal-footer">

                    {{#if canDonate}}
                     <form method="POST" action="#/cause/donate/{{id}}">
                        <label class="grey-text font-weight-light">My donation: </label>
                        <input name="donatedAmount" type="number" min="1" class="form-control" />
                        <button style="width: 100%;" class="btn btn-success" type="submit">Make the donation</button>
                    </form>
                    {{else}}
                      <a href="#/cause/close/{{id}}" class="btn btn-danger">Close the cause</a>
                    {{/if}}
                  
                   
                </div>
            </div>
        </div>
    </div> --}}
<!-- ./Details View (Become a donor) -->